name: The Super Brain 🧠 & Hunter 🕵️‍♂️

on:
  schedule:
    - cron: '0 */6 * * *' # كل 6 ساعات
  workflow_dispatch:

jobs:
  evolve:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup Node.js
      uses: actions/setup-node@v3
      with:
        node-version: '18'

    # 1. تشغيل العقل (للأخبار)
    - name: Run Brain (News)
      run: node scripts/brain.js

    # 2. تشغيل الصياد (للمصادر)
    - name: Run Hunter (Sources)
      run: node scripts/hunter.js

    # 3. حفظ كل النتائج
    - name: Commit Everything
      run: |
        git config --global user.name 'Super Bot 🤖'
        git config --global user.email 'bot@sentient-sky.app'
        git add public/*.json
        git commit -m "🧠 System evolved: News & Sources updated" || echo "No changes"
        git push
